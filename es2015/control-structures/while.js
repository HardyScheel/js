// 'While' loops are good for basic looping.
// 'Do-While' loops are good when you wish to ensure that the body of the loop is executed at least once.

let counterVar = 0;
do {
    console.log(++counterVar);
} while (counterVar <= 3)


while (true) {
    // an inifite loop
    console.log(true);
}
