// @ts-check

/**
 * Multiply two numbers and get the result.
 * @param {number} a A number to multiply.
 * @param {number} b A number to multiply with.
 */
function multiply(a, b) {
    return a * b
}

console.log(multiply(3, 6));