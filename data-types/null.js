// null ist a data type. It has the value of 'null'. The value of 'null' represents a 'no value'.

let iamNull = null;

let amINotANumber = isNaN(iamNull); // false

console.log(iamNull);
console.log(amINotANumber);