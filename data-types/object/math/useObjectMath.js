let sin = Math.sin(3.5);

/**
 * Calculate the scope of a cirlce.
 * 
 * By using: 2 * Math.PI * r
 * @param {number} radius - The radius of the circle.
 */
function calcScopeCircle(radius) {
    return 2 * Math.PI * radius;
}